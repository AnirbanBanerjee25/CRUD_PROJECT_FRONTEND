function Footer() {
    return (
        <div className="bg-warning" style={{ position: "fixed", bottom: 0, width: "100%", padding: "10px 0", textAlign: "center" }}>
            <footer>
                <span>
                    MERN FULL STACK CRUD PROJECT
                </span>
            </footer>
        </div>
    );
}

export default Footer;
